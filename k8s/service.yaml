apiVersion: v1
kind: Service
metadata:
  name: ram-train-book
  namespace: default
  labels:
    app: ram-train-book
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: ram-train-book
